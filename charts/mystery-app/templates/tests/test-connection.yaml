apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mystery-app.fullname" . }}-test-connection"
  labels:
    {{- include "mystery-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mystery-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
