apiVersion: v1
kind: Service
metadata:
  name: {{ include "mystery-app.fullname" . }}
  labels:
    {{- include "mystery-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mystery-app.selectorLabels" . | nindent 4 }}
